version: '3'
services:
  api:
    build:
      context: ./api
    container_name: api
    ports:
      - "5000:5000"

  telegram_bot:
    build:
      context: ./bot
    container_name: telegram_bot
    environment:
      - TELEGRAM_TOKEN= # Укажите токен для бота
    depends_on:
      - api
